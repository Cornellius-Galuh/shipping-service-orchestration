server:
  port: 8085

spring:
  application:
    name: tracking-service
  mongodb:
    uri: mongodb+srv://trisetyogaluh:galuh123@clusterws.x3cvhbg.mongodb.net/tracking_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
